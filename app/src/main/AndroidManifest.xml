<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.recodream_aos.recordream">
    <!-- Permissions of internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="default_messaging_channel" />
    <meta-data
        android:name="firebase_messaging_auto_init_enabled"
        android:value="false" />
    <meta-data
        android:name="firebase_analytics_collection_enabled"
        android:value="false" />

    <application
        android:name=".application.RecordreamApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/approgo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/approgo"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecorDream"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name="com.recodream_aos.recordream.presentation.record.RecordActivity"
            android:exported="true" />
        <activity
            android:name="com.recodream_aos.recordream.presentation.MainActivity"
            android:exported="false" />
        <activity
            android:name="com.recodream_aos.recordream.presentation.login.LoginActivity"

            android:exported="false" />
        <activity
            android:name="com.recodream_aos.recordream.presentation.mypage.MypageActivity"
            android:exported="false" />
        <activity
            android:name="com.recodream_aos.recordream.presentation.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Kakao 로그인 -->
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Redirect URI: "kakao{NATIVE_APP_KEY}://oauth" -->
                <!-- scheme-example: "kakao{NATIVE_APP_KEY}" -->
                <data
                    android:host="oauth"
                    android:scheme="@string/appKey_manifest_kakao" />
            </intent-filter>
        </activity>
    </application>

</manifest>